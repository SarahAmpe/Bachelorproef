bachelorproef
dt
bachelorproef
dt
bachelorproef
pcolor((2*pi*freq(w))*d/c)
shading flat
pcolor((2*pi*freq(w))*d/c)
shading flat
pcolor((2*pi*1E6)*d/c)
shading flat
pcolor((2*pi*3E6)*d/c)
pcolor((2*pi*3E5)*d/c)
pcolor(d/c)
bachelorproef
pcolor(d/c)
s = H(:,25,25);
plot(t,s);
10/c
hold on
plot(t,signal);
plot(t,s./max(s));
plot(t,signal/max(signal);
plot(t,signal/max(signal));
hold on
plot(t,s./max(real(s)));
5.495-7.468
p = hilbert(real(s));
plot(t,p./max(p));
plot(t,p./max(real(p)));
plot(t,imag(p)./max(real(p)));
plot(t,imag(p)./max(imag(p)));
reshape(H,1000,numElements^2)
envelope(reshape(H,1000,numElements^2));
envelope(reshape(real(H),1000,numElements^2));
s = H(:,25,25);
plot(t,s)
s = H(:,25,25);
plot(t,s)
se = H(:,25,25);
plot(t,se)
hold on
plot(t,s)
bachelorproef